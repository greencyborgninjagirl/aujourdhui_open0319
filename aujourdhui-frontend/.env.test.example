# 内测用：复制为 .env.development.local（开发模式）或 .env.test（构建模式）
# 将下方 IP 改为你电脑的局域网 IP
# 开发：复制为 .env.development.local，然后 npm run dev:h5
# 构建：复制为 .env.test，然后 npm run build:h5 -- --mode test
VITE_API_BASE=http://192.168.1.100:8000
